{
  "name": "xpos-main-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix pos-frontend run dev",
    "print": "npm --prefix pos-print-agent start",
    "dev:print": "npm --prefix pos-print-agent start",
    "dev:all": "concurrently \"npm --prefix pos-frontend run dev\" \"npm --prefix pos-backend run start:dev\"",
    "dev:backend": "npm --prefix pos-backend run start:dev",
    "postinstall": "cd pos-backend && npm install && npx prisma generate",
    "build": "cd pos-backend && npm run build",
    "start": "cd pos-backend && npm run start:prod"
  }
}
