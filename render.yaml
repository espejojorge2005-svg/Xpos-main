services:
  - type: web
    name: xpos-backend
    env: node
    plan: free
    rootDir: pos-backend
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
